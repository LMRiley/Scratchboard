<!DOCTYPE HTML>
<html lang="en">
<div class="container-fluid">
	<div class= "row-fluid" >
		<div class = "span3">
			&nbsp;
		</div>
		<div class = "span6">
		</br>		
			<div class = "well">
				<div align = "center">		
					<%= twitter_bootstrap_form_for [@project, @story], do |s| %>
						<%= s.inputs 'Edit Story' do %>
						</br>
						<%= s.text_area :content, "__", :size => "20x4" %>
						<%= s.actions do %>
							<%= s.submit 'Save', :class => 'btn'%>
						<% end %>
						<% end %>
					<% end %>
				</div>
				<div align = "right">
					<%= link_to 'delete', project_story_path(@project.id, @story.id), 
					:method => :delete, :confirm => "Are you sure you want to delete this story?" %>
				</div>
			</div>
		</div>
		<div class = "span3">
			&nbsp;
		</div>
	</div>
</div>
</html>